# j-flow Environment Configuration
# Copy this file to .env.local and adjust values as needed.

# No required environment variables — j-flow runs with defaults.
# JDK 17+ must be installed and `javac`/`java` must be on your PATH
# for the server-side Java compilation feature (/api/compile).
